[defaults]
remote_user = root
remote_tmp = /tmp
interpreter_python = /usr/bin/python
timeout = 30
host_key_checking = False
callback_whitelist = profile_tasks, timer

[ssh_connection]
pipelining = True
